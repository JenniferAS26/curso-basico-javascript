var nombre = "Santiago";

// declarar: reservame un espacio en memoria con este nombre y despues te voy a dar el valor
var edad;

// inicializar: poner el valor, traemos el nombre de ese espacio y le asignamos un valor. Si queremos darle un valor a esa variable utilizamos el simbolo del "=" solo un "=", hace referencia a asignar un valor
edad = 30;

var elementos = ["Computadora", "Celular"];
// para acceder a los elementos de este arreglo usamos la notacion elemento[posicion iniciando desde cero]

var persona = {
    nombre: "Jennifer",
    edad: 27
}

persona; // imprime directamente todo lo que tiene esta variable


